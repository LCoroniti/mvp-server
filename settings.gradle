plugins {
    // Allows Gradle to automatically download the JDK required by the
    // java toolchain (Java 22) if it is not installed locally.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'mvp-server'

include 'model'
include 'scraper'
include 'server'
include 'log'
include 'util'